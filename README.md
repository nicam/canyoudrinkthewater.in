Cloudflare VueJS Example
========================


# Setup
Copy `wrangler.toml.dist` to `wrangler.toml` and add your cloudflare id.


# Start
    npm install
    npm start

# Deploy
Make sure you have wrangler installed and your `wrangler.toml` setup.

    npm publish